[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
	"integration: pruebas que tocan recursos externos",
	"performance: pruebas de rendimiento que pueden ser largas",
]
